import RegisterClient from "./RegisterClient";

export const metadata = {
  title: "Register - Cleaning Hub",
  description: "Create a new account with Cleaning Hub to manage your bookings and access loyalty rewards.",
};

export default function RegisterPage() {
  return <RegisterClient />;
}
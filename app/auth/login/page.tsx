import LoginClient from "./LoginClient";

export const metadata = {
  title: "Login - Cleaning Hub",
  description: "Sign in to your Cleaning Hub account to manage bookings, view loyalty points, and access your dashboard.",
};

export default function LoginPage() {
  return <LoginClient />;
}